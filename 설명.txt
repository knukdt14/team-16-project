ui - 스트림릿 아니어도 됨
llm 모델도 제한 없음(가능한 로컬 구동)
RAG = 로컬 지식(유의미한, LLM 모델 성능에만 의존하지 않는)

git(github) -> github actions -> docker -> huggingface spaces

차종 지역 -->  보조금

docker file
docker-compose.yaml
          ↓
docker compose up --build
이걸로 pip install 이런 입력 없이 서비스 환경 설정 자동화
          ↓
github code spaces